/*
## 1. Soma simples
Crie uma função chamada `soma()` que declara duas variáveis locais `a` e `b`, 
soma os dois e imprime o resultado.
*/

#include <stdio.h> 


int soma(int a, int b){
    int resultado = a + b; 
    printf("O resultado da soma entre %d, e %d é; %d", a, b, resultado); 
}

int main() {

    return 0; 
}

/*
    Criei uma funcao chamada soma que contem 3 varáveis locais (int a, int b, int resultado), 
    estas variaveis nao podem ser acessadas fora da minha caso eu nao as declare lá dentro. 
*/



