{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Compilar e Executar C",
        "type": "shell",
        "command": "clang '${file}' -o '${fileDirname}/${fileBasenameNoExtension}' && '${fileDirname}/${fileBasenameNoExtension}'",
        "options": {
          "shell": {
            "executable": "/bin/bash",
            "args": ["-c"]
          }
        },
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": [],
        "detail": "Compila e executa o arquivo C atual"
      }
    ]
  }
  